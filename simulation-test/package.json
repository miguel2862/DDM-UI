{
  "name": "ddm-simulation-test",
  "version": "1.0.0",
  "description": "Standalone TypeScript port of the DiffDiscM simulation engine — for validation against R",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "tsc && node dist/compare.js",
    "sim": "tsc && node dist/run-extinction.js"
  },
  "devDependencies": {
    "@types/node": "^25.2.2",
    "typescript": "~5.5.0"
  }
}
